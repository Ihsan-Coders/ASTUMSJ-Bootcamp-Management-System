PORT=5000

# MongoDB Atlas connection string (create a free cluster, then paste the
# "Connect your application" URI here with your db user's credentials)
MONGO_URI=mongodb+srv://awela1499_db_user:r1L2gm1UQQRkJ0Fr@cluster0.dvsk9t0.mongodb.net

# Comma-separated list of origins allowed to call this API
CORS_ORIGINS=http://localhost:5173

# Signing secret for admin JWTs (generate with: openssl rand -hex 32)
JWT_SECRET=
JWT_EXPIRES_IN=3d

# Cloudinary (product image storage) — added in the Product API milestone
CLOUDINARY_CLOUD_NAME=waucgoia
CLOUDINARY_API_KEY=395789843777645
CLOUDINARY_API_SECRET=Jc7vI8QcUE3Kayfjq9sfMiuE7YY

# Frontend URL — used to build the password-reset link sent by email.
# Must match wherever the React app is actually running/deployed.
CLIENT_URL=http://localhost:5173

# SMTP config for the forgot-password email (nodemailer). For local dev,
# a free Mailtrap inbox (mailtrap.io) is the easiest way to test without
# sending real email. For Gmail, use an App Password, not your real one.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@astumsj-bootcamp.com
